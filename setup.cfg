[flake8]
max-line-length = 110
extend-ignore = E203, W503
exclude =
    .git,
    .venv,
    __pycache__,
    jars,
    output

[tool:pytest]
testpaths = tests
markers =
    kafka: end-to-end test requiring a real Kafka broker (KAFKA_BOOTSTRAP).
